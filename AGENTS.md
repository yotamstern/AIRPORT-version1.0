# GAP Project: Multi-Agent Handoff

## Current Objective:
Optimize the Genetic Algorithm memory usage and reflect Hard Penalty violations visually in the Gantt Chart.

## Role Assignment:
- **Claude Code (Terminal):** Strictly handle Backend Math (`Model` package). Focus on `GeneticAlgorithm.java`, `FitnessEvaluator.java`, and `FlightMinHeap.java`. 
- **Antigravity Agent (IDE):** Strictly handle Frontend & UI (`View` and `Controller` packages). Focus on `GanttChartPanel.java` and `SimulationController.java`.

## Pending Updates (Claude will write here, Antigravity will read from here):
- [ ] *Waiting for Claude to finish math optimization...*