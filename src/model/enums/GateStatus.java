package model.enums;

/**
 * Enum representing the current status of a gate.
 * Used to track availability for flight assignment.
 */
public enum GateStatus {
    FREE,
    OCCUPIED,
    MAINTENANCE
}
